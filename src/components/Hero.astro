<section class="hero">
  <div class="blob"></div>
  <div class="content">
    <p class="status">// SYSTEM_ONLINE</p>
    <h1>CL1NICAL</h1>
    <p class="tagline">Get on minecraft tn </p>
    <div class="links">
      <span>Projects</span>
      <span>Github</span>
      <span>Contact</span>
    </div>
  </div>
</section>

<style>
  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  /* The "Moving Part" - Glowing Background */
  .blob {
    position: absolute;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--accent-color) 0%, transparent 70%);
    filter: blur(80px);
    opacity: 0.4;
    z-index: 1;
    animation: move 10s infinite alternate;
  }

  .content {
    z-index: 2;
    text-align: center;
  }

  h1 {
    font-size: clamp(4rem, 15vw, 10rem);
    margin: 0;
    letter-spacing: -5px;
    font-weight: 900;
  }

  .status {
    font-family: monospace;
    color: var(--accent-color);
    letter-spacing: 2px;
    font-size: 0.9rem;
  }

  .tagline {
    color: #888;
    font-size: 1.2rem;
    margin-top: -10px;
  }

  .links {
    margin-top: 30px;
    display: flex;
    gap: 20px;
    justify-content: center;
    font-family: monospace;
    font-size: 0.8rem;
    color: #444;
  }

  /* Animation for the blob */
  @keyframes move {
    from { transform: translate(-20%, -20%); }
    to { transform: translate(20%, 20%); }
  }
</style>