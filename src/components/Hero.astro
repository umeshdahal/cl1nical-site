<section class="hero">
  <div class="glow-container">
    <div class="blob purple"></div>
    <div class="blob blue"></div>
  </div>
  
  <div class="grid-overlay"></div>

  <div class="content">
    <div class="glass-card">
      <p class="status">
        <span class="pulse"></span> 
        STATUS: <span class="typing">EVOLVING_DIGITAL_SPACE</span>
      </p>
      
      <h1 class="glitch" data-text="CL1NICAL">CL1NICAL</h1>
      
      <p class="description">
        Developer // Self-Hoster // Open Source Purist
      </p>

      <div class="stats-grid">
        <div class="stat-item">
          <span class="label">ENGINE</span>
          <span class="value">ASTRO 4.0</span>
        </div>
        <div class="stat-item">
          <span class="label">UPTIME</span>
          <span class="value">99.9%</span>
        </div>
        <div class="stat-item">
          <span class="label">LOC</span>
          <span class="value">12.4k</span>
        </div>
      </div>

      <div class="action-buttons">
        <a href="#" class="btn primary">View Projects</a>
        <a href="#" class="btn secondary">Get In Touch</a>
      </div>
    </div>
  </div>
</section>

<style>
  :root {
    --purple: #8833ff;
    --blue: #0066ff;
    --glass: rgba(255, 255, 255, 0.03);
    --border: rgba(255, 255, 255, 0.1);
  }

  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    background: #050505;
    overflow: hidden;
  }

  /* 1. Moving Background Elements */
  .glow-container {
    position: absolute;
    inset: 0;
    filter: blur(100px);
    z-index: 1;
  }

  .blob {
    position: absolute;
    width: 600px;
    height: 600px;
    border-radius: 50%;
    opacity: 0.3;
  }

  .purple {
    background: var(--purple);
    top: -10%;
    left: -10%;
    animation: float 20s infinite alternate;
  }

  .blue {
    background: var(--blue);
    bottom: -10%;
    right: -10%;
    animation: float 25s infinite alternate-reverse;
  }

  /* 2. Grid Overlay for "Cyber" look */
  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(var(--border) 1px, transparent 1px),
                      linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 50px 50px;
    mask-image: radial-gradient(circle, black, transparent 80%);
    z-index: 2;
  }

  /* 3. The Main Glass Card */
  .glass-card {
    position: relative;
    z-index: 10;
    padding: 3rem;
    background: var(--glass);
    backdrop-filter: blur(20px);
    border: 1px solid var(--border);
    border-radius: 24px;
    text-align: center;
    max-width: 600px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }

  h1 {
    font-size: 5rem;
    font-weight: 900;
    margin: 1rem 0;
    letter-spacing: -2px;
    color: white;
  }

  .status {
    font-family: monospace;
    font-size: 0.8rem;
    color: var(--purple);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .pulse {
    width: 8px;
    height: 8px;
    background: #00ff88;
    border-radius: 50%;
    box-shadow: 0 0 10px #00ff88;
    animation: pulse 2s infinite;
  }

  .description {
    color: #aaa;
    font-size: 1.1rem;
    margin-bottom: 2rem;
  }

  /* 4. Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    padding: 1.5rem;
    background: rgba(0,0,0,0.3);
    border-radius: 12px;
    margin-bottom: 2rem;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
  }

  .label { font-size: 0.6rem; color: #666; font-weight: bold; }
  .value { font-size: 0.9rem; color: white; font-family: monospace; }

  /* 5. Buttons */
  .action-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .btn {
    padding: 0.8rem 1.5rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: bold;
    text-decoration: none;
    transition: 0.3s;
  }

  .primary { background: white; color: black; }
  .primary:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(255,255,255,0.1); }

  .secondary { color: white; border: 1px solid var(--border); }
  .secondary:hover { background: var(--border); }

  /* Animations */
  @keyframes float {
    0% { transform: translate(0, 0); }
    100% { transform: translate(100px, 50px); }
  }

  @keyframes pulse {
    0% { opacity: 1; }
    50% { opacity: 0.3; }
    100% { opacity: 1; }
  }
</style>